<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.renren.modules.sys.dao.SysUserDao">

    <!-- 查询用户的所有权限 -->
    <select id="queryAllPerms" resultType="string">
        select m.perms
        from sys_user_role ur
                 LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
                 LEFT JOIN sys_menu m on rm.menu_id = m.menu_id
        where ur.user_id = #{userId}
    </select>

    <!-- 查询用户的所有菜单ID -->
    <select id="queryAllMenuId" resultType="long">
        select distinct rm.menu_id
        from sys_user_role ur
                 LEFT JOIN sys_role_menu rm on ur.role_id = rm.role_id
        where ur.user_id = #{userId}
    </select>

    <select id="queryByUserName" resultType="io.renren.modules.sys.entity.SysUserEntity">
        select *
        from sys_user
        where username = #{username}
    </select>

    <!--查询用户列表-->
    <select id="queryUserList" resultType="io.renren.modules.sys.vo.UserListVO">
        SELECT a.user_id as userId, a.username, b.area_name as areaName, a.type, a.mobile, a.`status`, a.create_time as
        createTime
        FROM sys_user a
        INNER JOIN tb_area b
        ON a.area_id = b.id
        <where>
            <if test="username != null and username != ''">
                a.username like '%${username}%'
            </if>
            <if test="createUserId != null and createUserId != ''">
                and a.create_user_id = #{createUserId}
            </if>
        </where>
    </select>
</mapper>